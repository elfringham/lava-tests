metadata:
    name: laa-docker-tests
    format: "Manual Test Definition 1.0"
    description: "Tests for running in docker on LAA"
    maintainer:
        -andrew.goodbody@linaro.org
    os:
        - ubuntu
run:
    steps:
        - "uname -a"
        - "echo $GITLAB_PRIVATE"
